{
  "build": {
    "env": {
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  },
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "rm -rf .next/cache/webpack .pnpm-store && pnpm run build"
}
